# Install the torch and torch_npu wheels that exactly match the server's CANN
# release first. They are intentionally not pinned here because the supported
# pair is determined by the installed Ascend software stack.
-r requirement.txt
